# API key de Anthropic (Claude)
ANTHROPIC_API_KEY=sk-ant-api03-tu-clave

# LangSmith (observabilidad / tracing)
LANGSMITH_API_KEY=lsv2_pt_tu-clave
LANGSMITH_ENDPOINT=https://eu.api.smith.langchain.com
LANGSMITH_TRACING=true
LANGSMITH_PROJECT=eurofinder

# En CLI/tests ayuda a que el proceso espere a subir los spans antes de cerrar.
LANGCHAIN_CALLBACKS_BACKGROUND=false

# Opcional: Leboncoin usa Datadome y puede bloquear IPs de datacenter/VPN.
# Si devuelve 403, usa un proxy limpio, idealmente residencial o movil de FR.
# Formato: http://usuario:password@host:puerto
LEBONCOIN_PROXY_URL=
LEBONCOIN_IMPERSONATE=chrome146
